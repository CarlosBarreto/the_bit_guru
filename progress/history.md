# progress/history.md — Bitácora append-only

> Cada sesión cerrada deja aquí una entrada. No se edita el pasado, solo se
> añade al final.

---

<!--
Plantilla de entrada (copiar al cerrar sesión):

## YYYY-MM-DD — feature <id> <name>

- **Veredicto:** done | blocked
- **Resumen:** (1-3 líneas)
- **Archivos tocados:** lista breve
- **Notas para la próxima sesión:** (si las hay)

---
-->

## 2026-05-29 — feature 1 setup_astro_unified

- **Veredicto:** done
- **Resumen:** Astro habilitado en modo SSR (`output: 'server'`) con adapter `@astrojs/vercel` (pineado a `^9` por peer astro@^5); instaladas deps `@google/genai`, `vitest` + toolchain de lint; scripts npm `dev/build/preview/test/lint`. Build verde sin endpoints, smoke test verde, `init.ps1` exit 0. Aprobado por reviewer.
- **Archivos tocados:** `frontend-astro/astro.config.mjs`, `frontend-astro/package.json`, `frontend-astro/package-lock.json`, `frontend-astro/eslint.config.js`, `frontend-astro/tsconfig.json`, `frontend-astro/tests/smoke.test.ts`, `.gitignore`.
- **Notas para la próxima sesión:** La siguiente feature pending es la 2 `lib_persona_and_tarot` (capa de dominio: `lib/persona.ts` + `lib/tarot.ts`). Deuda fuera de scope: `eslint.config.js` importa `globals` sin declararlo como devDependency explícita, y `npm run lint` deja 3 errores en scaffold preexistente (`mini-oraculo.astro`, `zoltar.astro`).

## 2026-05-29 — feature 2 lib_persona_and_tarot

- **Veredicto:** done
- **Resumen:** Capa de dominio pura: `lib/persona.ts` (exporta `PERSONA_BASE` + `buildSystemPrompt(opts)`, derivado de PERSONA.md con género masculino y tono cómplice cínico) y `lib/tarot.ts` (constante `ARCANOS` con los 22 arcanos cibernéticos, idénticos carácter por carácter al canon del PHP en `legacy/`). Cero imports de HTTP/Astro/Gemini. `.\init.ps1` verde: 3 test files, 11 tests passing. Aprobado por reviewer.
- **Archivos tocados:** `frontend-astro/src/lib/persona.ts`, `frontend-astro/src/lib/tarot.ts`, `frontend-astro/tests/lib/persona.test.ts`, `frontend-astro/tests/lib/tarot.test.ts`.
- **Notas para la próxima sesión:** La siguiente feature pending es la 3 `lib_gemini_wrapper` (wrapper único de Gemini en `lib/gemini.ts`, mockeable desde tests, lee API key de `import.meta.env.GEMINI_API_KEY`). Deuda heredada de feature 1 aún abierta (lint en scaffold preexistente).

## 2026-05-29 — feature 3 lib_gemini_wrapper

- **Veredicto:** done
- **Resumen:** Wrapper único de Gemini en `lib/gemini.ts`: `generate(prompt, opts)` encapsula el SDK `@google/genai` (`GoogleGenAI` → `models.generateContent`), lee la key solo de `import.meta.env.GEMINI_API_KEY` (error descriptivo si falta, antes de instanciar el SDK) y soporta opts model/systemInstruction/temperature/maxOutputTokens. SDK mockeado con `vi.mock()`; ningún otro archivo de src importa el SDK. `.\init.ps1` verde: 4 test files, 21 tests passing (10 nuevos). Aprobado por reviewer.
- **Archivos tocados:** `frontend-astro/src/lib/gemini.ts`, `frontend-astro/tests/lib/gemini.test.ts`.
- **Notas para la próxima sesión:** La siguiente feature pending es la 4 `endpoint_morpheus_quotes` (GET `/api/morpheus-quotes`: array fijo de frases tipo Morpheus, sin Gemini; primer smoke del setup Vercel). Deuda heredada de feature 1 aún abierta (lint en scaffold preexistente).

## 2026-05-29 — feature 4 endpoint_morpheus_quotes

- **Veredicto:** done (smoke curl del dueño pendiente — no bloqueante)
- **Resumen:** Endpoint estático `GET /api/morpheus-quotes` (`export const GET: APIRoute`, try/catch, `Response` 200 `application/json`) que devuelve `MORPHEUS_QUOTES`, las 8 frases tipo Morpheus copiadas byte-a-byte del canon PHP (`TarotController.php::morpheusQuotes()`). Sin Gemini, sin `lib/`. `.\init.ps1` verde: 5 test files, 24 tests passing. Aprobado por reviewer.
- **Archivos tocados:** `frontend-astro/src/pages/api/morpheus-quotes.ts`, `frontend-astro/tests/pages/api/morpheus-quotes.test.ts`.
- **Pendiente de dueño:** smoke con curl. Comando exacto del informe del implementer:
  ```powershell
  npm run dev --prefix frontend-astro
  # en otra terminal:
  curl http://localhost:4321/api/morpheus-quotes
  ```
  Esperado: HTTP 200, `Content-Type: application/json`, array JSON de 8 strings empezando por "La Matrix está en todas partes...".
- **Notas para la próxima sesión:** La siguiente feature pending es la 5 `endpoint_tirada` (GET `/api/tirada`: selecciona 3 arcanos al azar sin reemplazo de `lib/tarot.ts`; solo lógica de dominio, sin Gemini). Deuda heredada de feature 1 aún abierta (lint en scaffold preexistente).

## 2026-05-29 — feature 5 endpoint_tirada

- **Veredicto:** done
- **Resumen:** Endpoint `GET /api/tirada` que devuelve 3 arcanos distintos (sin reemplazo) tomados de `ARCANOS` (lib/tarot.ts) vía barajado parcial Fisher-Yates sobre copia + `slice(0,3)`. Sin Gemini, solo lógica de dominio. Tests verdes (cardinalidad 3 + unicidad sobre 100 ejecuciones). Aprobado por reviewer.
- **Archivos tocados:** frontend-astro/src/pages/api/tirada.ts, frontend-astro/tests/pages/api/tirada.test.ts
- **Notas:** lote paralelo (5,6,8)

## 2026-05-29 — feature 6 endpoint_pregunta

- **Veredicto:** done (smoke curl + clave real del dueño pendiente — no bloqueante)
- **Resumen:** Endpoint `POST /api/pregunta` que recibe `{ pregunta: string }`, arma el system prompt con `buildSystemPrompt` (lib/persona) y genera la respuesta del Gurú vía `generate` (lib/gemini). Validación 400 (falta/no-string/vacío/body no-JSON), 502 si Gemini falla, 500 inesperado; mensajes neutros sin filtrar internals. 7 tests verdes con `vi.mock()` del wrapper. Aprobado por reviewer.
- **Archivos tocados:** frontend-astro/src/pages/api/pregunta.ts, frontend-astro/tests/pages/api/pregunta.test.ts
- **Pendiente de dueño:** smoke curl con clave real:
  ```bash
  npm run dev --prefix frontend-astro
  curl -X POST http://localhost:4321/api/pregunta \
    -H "Content-Type: application/json" \
    -d '{"pregunta": "¿voy a triunfar en redes?"}'
  ```
- **Notas:** lote paralelo (5,6,8)

## 2026-05-29 — feature 8 endpoint_wisdom_tweet

- **Veredicto:** done (smoke local del dueño pendiente — no bloqueante)
- **Resumen:** Endpoint `GET /api/wisdom-tweet` con `?tema=<x>` opcional; devuelve `{ tweet: string }` con length <= 280 (instrucción al prompt + truncado defensivo `truncateTweet`). Usa `buildSystemPrompt` + `generate`; 502 si Gemini falla, 500 inesperado, mensajes neutros. 7 tests verdes con `vi.mock()` del wrapper. NOTA: el implementer se cortó por error de socket antes de escribir su informe; el reviewer verificó que el código quedó íntegro (sin truncamiento, sin TODO/FIXME) y aprobó. NO existe progress/impl_endpoint_wisdom_tweet.md.
- **Archivos tocados:** frontend-astro/src/pages/api/wisdom-tweet.ts, frontend-astro/tests/pages/api/wisdom-tweet.test.ts
- **Pendiente de dueño:** smoke local
- **Notas:** lote paralelo (5,6,8)

## 2026-05-29 — feature 7 endpoint_reading

- **Veredicto:** done (smoke local del dueño pendiente — no bloqueante)
- **Resumen:** Endpoint `POST /api/reading` que sortea 3 arcanos distintos (sin reemplazo) de `lib/tarot` vía Fisher-Yates parcial y genera la interpretación en tono del Gurú con `buildSystemPrompt` (lib/persona) + `generate` (lib/gemini); devuelve `{ arcanos: [...3], interpretacion: string }`. 502 si Gemini falla, 500 inesperado, mensajes neutros. Tests mockean el wrapper `lib/gemini` y validan estructura, 3 arcanos distintos del canon y que viajan al system prompt. Aprobado por reviewer.
- **Archivos tocados:** frontend-astro/src/pages/api/reading.ts, frontend-astro/tests/pages/api/reading.test.ts
- **Notas:** lote paralelo (7,9,10)

## 2026-05-29 — feature 9 endpoint_fan_response

- **Veredicto:** done (smoke local del dueño pendiente — no bloqueante)
- **Resumen:** Endpoint `POST /api/fan-response` que recibe `{ mensaje: string, contexto?: string }` y responde en tono cómplice cariñoso. La marca §6 'fan emocionado → cómplice cariñoso' se inyecta vía el `task` del endpoint (no se hardcodea en el test) y se valida sobre el `systemInstruction` real que recibe `generate`. 400 input inválido, 502 Gemini caído, 500 inesperado; mensajes neutros. `lib/persona.ts` y `lib/gemini.ts` intactos. Aprobado por reviewer.
- **Archivos tocados:** frontend-astro/src/pages/api/fan-response.ts, frontend-astro/tests/pages/api/fan-response.test.ts
- **Notas:** lote paralelo (7,9,10)

## 2026-05-29 — feature 10 endpoint_create_image

- **Veredicto:** done
- **Resumen:** Endpoint `POST /api/create-image` que construye un prompt textual determinístico (sin Gemini) con la paleta canónica (morado, cyan, negro, neón rosa) y prohibición explícita de fotorrealismo y caras humanas completas (§8 PERSONA); devuelve `{ prompt: string }`. 400 input inválido, 500 con mensaje neutro. Tests validan presencia de palabras clave de paleta y restricciones. Aprobado por reviewer.
- **Archivos tocados:** frontend-astro/src/pages/api/create-image.ts, frontend-astro/tests/pages/api/create-image.test.ts
- **Notas:** lote paralelo (7,9,10). Con esto los 7 endpoints (4,5,6,7,8,9,10) están done; siguiente: feature 11 deploy_vercel_preview.

## 2026-05-30 — feature 11 deploy_vercel_preview
- **Veredicto:** done
- **URL preview:** https://the-bit-guru-git-feat-migrate-to-vercel-carlosbarretos-projects.vercel.app (commit 117990f)
- **Resumen:** Deploy SSR a Vercel (proyecto the-bit-guru, Root Directory=frontend-astro). Smoke de los 7 endpoints: 7/7 en HTTP 200 con respuestas en tono del Gurú.
- **Aprendizajes:** (1) repo renombrado bitsGuru->the_bit_guru por mayúsculas; remote local actualizado. (2) API key inicial con free tier limit:0 (429); key nueva sin restricción + redeploy lo resolvió; las env vars de Vercel se inyectan en build, requieren redeploy. (3) Deployment Protection de Vercel daba 401 a curl; se desactivó para el preview.
- **Pendiente de dueño (no bloqueante):** considerar re-activar Deployment Protection; pasar GEMINI_API_KEY a frontend-astro/.env si se quiere dev local con Gemini.
- **Notas para la próxima sesión:** siguiente pending = feature 12 drop_legacy_php (depende de este preview verde) y feature 13 ui_port_astro.

## 2026-05-30 — feature 12 drop_legacy_php
- **Veredicto:** done
- **Resumen:** Borrado de legacy/backend-php/ (7 archivos PHP) tras validar el preview Vercel verde. .gitignore limpiado de referencias a legacy. El snapshot PHP queda preservado en el tag v0.1-php-final (apunta a d3879d9, en origin).
- **Archivos tocados:** eliminado legacy/backend-php/**, modificado .gitignore
- **Nota:** el commit de borrado (lo hace el leader) referencia el tag v0.1-php-final.
- **Notas para la próxima sesión:** única pending = feature 13 ui_port_astro (porte de UI desde el mockup de Stitch; ver docs/design/).

## 2026-05-30 — feature 13 ui_port_astro
- **Veredicto:** done
- **Resumen:** Porte de la UI del mockup de Stitch a Astro con CSS propio + design tokens canónicos (PERSONA §8). Landing = Layout (tokens+fuentes+prefers-reduced-motion) + index.astro ensamblando 6 componentes: Nav, Hero (typewriter cínico + SVG encapuchado), TarotSection (3 hex, /api/tirada, CTA rosa único), Consulta (textarea → /api/pregunta), Sabiduria (/api/wisdom-tweet, @ElGuruDeBits), Footer (taglines cínicos, sin año). Filo cínico + paleta canónica + arcanos reales + sin fotorrealismo + sin Tailwind CDN.
- **Estrategia:** fundación serial (contrato: tokens, index, stubs, infra test Container API) → lote paralelo de 3 implementers (2 componentes c/u) → 3 reviewers en paralelo.
- **Archivos tocados:** Layout.astro, index.astro, src/components/{Nav,Hero,TarotSection,Consulta,Sabiduria,Footer}.astro, vitest.config.ts, tests/components/*.
- **Hito:** 13/13 features done — migración bit_guru a Astro/Vercel COMPLETA.
- **Pendiente de dueño:** re-activar Deployment Protection en Vercel; smoke visual de la landing en el preview; (opcional) páginas viejas oraculo/zoltar/etc. siguen en el repo, fuera de scope de la migración.

## 2026-06-08 — feature 14 redesign_tokens_typography
- **Veredicto:** done
- **Resumen:** Phase 1 del rediseño editorial. Se reemplazó el bloque de tokens neón de `Layout.astro` (`--bg #0a0a0f`, `--morado #7b2cbf`, `--cyan #00f0ff`, `--rosa #ff2d95` + fuentes Plus Jakarta/Be Vietnam/Playfair/JetBrains) por el CSS token system de `docs/design/redesign_advisory_spec.md`: neutrals (papel marfil `--paper*`), navy/tinta (`--ink*`), acentos burgundy/olive/forest, alias `--accent*` con default neutro Navy y remapeo por página vía `[data-accent="burgundy"|"olive"|"forest"]`, escala tipográfica ratio 1.25 (`--step--1…5`), espaciado 8px (`--space-1…10`), `--radius: 2px`, `--measure: 68ch`. El `<link>` de Google Fonts ahora carga solo Source Serif 4 + IBM Plex Sans + IBM Plex Mono. Cero hex de marca fuera de la tabla de tokens; sin gradiente/glow/dark mode; bloque `prefers-reduced-motion` conservado intacto.
- **Test:** nuevo test de contrato `tests/layouts/layout-tokens.test.ts` (presencia de todos los tokens + hex canónicos + 3 fuentes + 3 selectores `[data-accent]`; ausencia de hex neón viejos, gradientes CSS, Tailwind CDN y las 4 fuentes obsoletas). `foundation.test.ts` ajustado a los tokens nuevos sin alterar pruebas de slot/lang/no-CDN.
- **Verificación:** `init.ps1` verde (176/176 tests, 19 files) y `npm run build` verde (server + client + Vercel). Aprobado por reviewer (`progress/review_14.md`, sin cambios requeridos).
- **Archivos tocados:** `frontend-astro/src/layouts/Layout.astro`, `frontend-astro/tests/layouts/layout-tokens.test.ts`, `frontend-astro/tests/components/foundation.test.ts`.
- **Notas para la próxima sesión:** Alcance acotado a `Layout.astro` + sus tests; los componentes/páginas que aún referencian `var(--morado)` etc. se reescriben en features 16–26 (la 15 `redesign_layout_primitives` es la siguiente pending). El aserto de gradiente verifica las funciones CSS `*-gradient` (prohibición precisa de la spec), no la subcadena "gradient".

## 2026-06-08 — feature 15 redesign_layout_primitives
- **Veredicto:** done
- **Resumen:** Phase 1 del rediseño editorial (cimientos de layout). Tres primitivos de layout reusables, todos consumiendo SOLO los tokens de `Layout.astro` (feature 14), a la izquierda y sin centrar prosa: (1) `Container.astro` — rejilla `grid-template-columns: repeat(12, 1fr)` (12 columnas), `max-width: 1200px` + `margin-inline: auto`, gutter `var(--space-5)` (24px), `padding-inline` de la escala (`--space-7`/`--space-6` ≤1023px/`--space-5` ≤640px con colapso a 1 columna); props `as` + `class`; (2) `Prose.astro` — wrapper de medida de lectura `max-width: var(--measure)` (68ch), `text-align: left` (sin centrar), `line-height: 1.6`, `font-family: var(--font-serif)`; (3) `SectionDivider.astro` — filete `border-top: 1px solid var(--ink-rule)` + eyebrow mono OPCIONAL (`var(--font-mono)`, uppercase, `letter-spacing: 0.08em`, `--step--1`, `color: var(--accent)`) que se omite del DOM sin `label`; `role="separator"`. Cero hex de marca, sin gradiente/glow/box-shadow/dark mode, sin Tailwind CDN; los primitivos no fijan familia de acento (usan `--accent`, default Navy, remapeable por página).
- **Test:** nuevo `tests/components/layout-primitives.test.ts` (14 tests) con el patrón doble del repo: Container API (`experimental_AstroContainer`) para markup (slot, clase, prop `as`, prop `class`, presencia/ausencia de eyebrow, `role="separator"`) + `readFileSync` del source para el contrato de `<style>` (rejilla 12-col, 1200px, gutter `--space-5`, `--measure`, izquierda, filete/eyebrow; ausencia de hex/gradiente/glow/CDN). No se introdujo mecanismo nuevo.
- **Verificación:** `init.ps1` verde (190/190 tests, 20 files) y `npm run build` verde (server + client + adapter Vercel). Aprobado por reviewer (`progress/review_15.md`, sin cambios requeridos).
- **Archivos tocados:** `frontend-astro/src/components/Container.astro`, `frontend-astro/src/components/Prose.astro`, `frontend-astro/src/components/SectionDivider.astro`, `frontend-astro/tests/components/layout-primitives.test.ts`.
- **Notas para la próxima sesión:** Alcance acotado a los 3 primitivos + sus tests; no se tocó Nav/Footer/Hero/páginas, ni `lib/`, ni `pages/api/`. Siguiente pending = feature 16 `redesign_nav_footer` (Phase 2: TopNavigation de 6 enlaces + Footer en registro consultora). Los primitivos quedan listos para que las páginas 19–25 los consuman.

## 2026-06-08 — feature 16 redesign_nav_footer
- **Veredicto:** done
- **Resumen:** Phase 2 del rediseño editorial (chrome compartido al registro consultora deadpan de `docs/design/redesign_advisory_spec.md`). Tres archivos: (1) `nav-links.ts` — fuente de verdad única de las EXACTAMENTE 6 rutas en el orden de la spec § SITE MAP (`/practica`, `/metodologia`, `/memos`, `/archivo`, `/la-firma`, `/admision`), `HOME_HREF="/"` y `normalizePath()` para el estado activo (`/memos/`==`/memos`); (2) `TopNavigation.astro` (componente nuevo, nombre de la spec § REQUIRED COMPONENTS) — `<header id="top">` sticky discreto + `<nav>` con wordmark "Bit Gurú · Asesoría Estratégica"→`/` (serif + sufijo mono), 6 enlaces desde `NAV_LINKS`, estado activo con `aria-current="page"` reforzado de forma no-cromática (peso 600 + filete), prop `pathname` (default `Astro.url.pathname`) para testabilidad, foco visible; (3) `Footer.astro` (reescrito in-place sobre el footer neón de la feature 13) — directorio de las mismas 6 secciones, disclaimer cínico-institucional, firma del Socio Fundador ("El Gurú de Bits — Socio Fundador. Operando desde los espacios muertos entre paquetes."), SIN año / sin "©". Solo tokens `--ink*/--paper*/--accent*/--space-*/--step-*/--font-*`; sin gradiente/glow/box-shadow/backdrop-filter/dark mode/Tailwind CDN. No se tocó `index.astro`, `Nav.astro`, `src/lib/` ni `src/pages/api/` (la limpieza de la UI neón y el recableado de páginas es alcance de la feature 26, Phase 5).
- **Test:** nuevo `tests/components/TopNavigation.test.ts` + reescrito `tests/components/Footer.test.ts`, ambos con el patrón doble del repo (Container API para markup + `readFileSync` del source para tokens/prohibiciones). Validan: 6 enlaces exactos + orden/rutas sobre `NAV_LINKS`, wordmark→/, `aria-current` solo en la ruta actual, sin activo en `/`, sticky, foco visible, disclaimer + firma del Socio Fundador, ausencia de año (`/(19|20)\d{2}/`), `©` y `&copy;`, sin hex sueltos / gradiente / box-shadow / backdrop-filter / Tailwind CDN / tokens neón.
- **Verificación:** `init.ps1` verde — 207/207 tests, 21 archivos (baseline previo 190/20; +1 archivo TopNavigation, +17 tests netos) — y `npm run build` verde (server + client + adapter Vercel). Aprobado por reviewer (`progress/review_16.md`, APPROVED, sin cambios requeridos).
- **Tokens consumidos:** no instrumentados en esta sesión (el arnés no registra consumo de tokens; las métricas disponibles son las de tests/build arriba).
- **Archivos tocados:** `frontend-astro/src/components/nav-links.ts` (nuevo), `frontend-astro/src/components/TopNavigation.astro` (nuevo), `frontend-astro/src/components/Footer.astro` (reescrito), `frontend-astro/tests/components/TopNavigation.test.ts` (nuevo), `frontend-astro/tests/components/Footer.test.ts` (reescrito).
- **Notas para la próxima sesión:** Alcance acotado al chrome compartido + sus tests. Siguiente pending = feature 17 `redesign_editorial_components_a` (EditorialHero + PullQuote). El `TopNavigation`/`Footer` nuevos aún no están cableados en ninguna página; su integración llega con las páginas 19–25 y la limpieza neón con la feature 26.

## 2026-06-08 — feature 17 redesign_editorial_components_a
- **Veredicto:** done
- **Resumen:** Phase 2 del rediseño editorial. Dos componentes de presentación reusables, consumiendo SOLO tokens de `Layout.astro` (feature 14), alineados a la izquierda y sin centrar prosa: (1) `EditorialHero.astro` — `<section>` + `<h1>` semánticos sin imagen de fondo; props `eyebrow?`, `title` (req), `deck?`, `ctaLabel?`, `ctaHref?`; eyebrow mono (MAYÚSCULAS, `letter-spacing 0.08em`, `--step--1`, `color var(--accent)`, omitido sin prop), H1 display serif (`clamp(var(--step-4), 5vw, var(--step-5))`, `line-height 1.2`, `color var(--ink-deep)`), deck serif (`--step-1`, `--ink-muted`, dentro de `--measure`, omitido sin prop), CTA sobrio (`background var(--accent)`, `border-radius var(--radius)` ≤2px, foco visible `outline 2px solid var(--accent)` + offset 2px, hover `--accent-bright`) OMITIDO del DOM sin `ctaHref`. (2) `PullQuote.astro` — `<blockquote>` + `<cite>` semánticos; props `quote` (req), `cite?`; filete de acento a la IZQUIERDA (`border-left: 3px solid var(--accent)` + `padding-left: var(--space-5)`), cita serif (`clamp(var(--step-2), 3vw, var(--step-3))`, `line-height 1.4`, `--ink-deep`), `<cite>` sans (`--step--1`, `--ink-muted`, `font-style: normal`, omitido sin prop), sin comillas decorativas. Ninguno fija familia de acento (usa `--accent`, lo remapea la página con `data-accent`). Cero hex de marca; sin gradiente/glow/box-shadow/text-shadow/dark mode/emoji/Tailwind CDN. No se tocó `lib/`, `pages/api/`, ni se crearon páginas/Nav/Footer/tarjetas.
- **Test:** nuevos `tests/components/editorial-hero.test.ts` + `tests/components/pull-quote.test.ts`, patrón doble del repo (réplica de `layout-primitives.test.ts`): Container API (`experimental_AstroContainer`) para markup (presencia/ausencia de eyebrow/deck/CTA según props, CTA ausente sin `ctaHref`; `<blockquote>` con quote, `<cite>` solo con `cite`) + `readFileSync` del source para el contrato de `<style>` (familia serif, escala display, filete `border-left 3px solid var(--accent)`, tokens; ausencia de hex/gradiente/glow). La auditoría de paleta se acota al bloque `<style>` (helper `styleBlock`) para no falsar por la palabra "gradiente" en los comentarios de cabecera que DECLARAN las prohibiciones.
- **Verificación:** `init.ps1` verde — 223/223 tests, 23 test files (baseline previo 207/21; +2 archivos, +16 tests netos) — y `npm run build --prefix frontend-astro` verde (server + client + adapter Vercel). Aprobado por reviewer (`progress/review_17.md`, APPROVED ciclo 1, sin cambios requeridos).
- **Tokens consumidos:** no instrumentados en esta sesión (el arnés no registra consumo de tokens; las métricas disponibles son las de tests/build arriba).
- **Archivos tocados:** `frontend-astro/src/components/EditorialHero.astro` (nuevo), `frontend-astro/src/components/PullQuote.astro` (nuevo), `frontend-astro/tests/components/editorial-hero.test.ts` (nuevo), `frontend-astro/tests/components/pull-quote.test.ts` (nuevo).
- **Notas para la próxima sesión:** Alcance acotado a los 2 componentes editoriales + sus tests. Siguiente pending = feature 18 `redesign_editorial_components_b` (InsightCard + MetricTable + PartnerBiographyBlock). `EditorialHero`/`PullQuote` aún no están cableados en ninguna página; su integración llega con las páginas 19–25.

## 2026-06-09 — feature 18 redesign_editorial_components_b
- **Veredicto:** done
- **Resumen:** Phase 2 del rediseño editorial. Tres componentes de presentación reusables nuevos, consumiendo SOLO los tokens de `Layout.astro` (feature 14), alineados a la izquierda y sin fijar familia de acento (usan `--accent`, remapeable por página vía `data-accent`): (1) `InsightCard.astro` — `<article>` con borde hairline `1px var(--ink-rule)` + filete superior de acento (`border-top: 2px solid var(--accent)`), `--radius`, fondo `--paper-pure`, sin sombra/glow; props `eyebrow?`, `title` (req, enlace con `href?` + foco visible), `excerpt?`, `date?`; eyebrow/fecha en mono `--step--1`, título serif `--step-2 var(--ink-deep)`, extracto serif. (2) `MetricTable.astro` — semántica de tabla REAL: `<table>` + `<caption>` opcional + `<thead>`/`<th scope="col">` + `<tbody>`/`<th scope="row">` por fila + `<td class="value">`, cero divs estructurales; props `caption?`, `columns: string[]`, `rows`; hairlines `1px var(--ink-rule)` + `border-collapse`, banda de encabezado `--ink-wash`, números en `--font-mono` (`tabular-nums`). (3) `PartnerBiographyBlock.astro` — retrato NO-facial: SVG inline a una tinta (silueta encapuchada SIN rostro + monograma "BG" tipo sello), `aria-hidden="true"`, cero `<img>`/fotorrealismo; props `name?`/`role?`/`bio?` (defaults canónicos en registro consultora, bio también vía slot); dos columnas en desktop (`120px 1fr`), apilado en móvil. Copy deadpan en registro consultora (usted). No se tocó `lib/`, `pages/api/`, páginas, Nav/Footer/Hero (diff vacío confirmado por el reviewer).
- **Test:** tres tests espejo nuevos con el patrón doble del repo (Container API `experimental_AstroContainer` para markup + `readFileSync` del source para tokens/prohibiciones): `tests/components/insight-card.test.ts` (11), `tests/components/metric-table.test.ts` (9: `<table>`/`<caption>`/`<th scope>` por columna y fila, "NO usa divs estructurales", hairlines, banda `--ink-wash`, mono), `tests/components/partner-biography.test.ts` (9: nombre/cargo defaults y props, sin `<img>`, SVG `aria-hidden`, slot, sello a una tinta sin rostro, dos columnas/apilado). Incidencias resueltas en sesión: ENOSPC transitorio del disco C: (se recuperó solo, archivo reescrito) y 4 tests rojos en la primera corrida por (a) el literal `<img` en comentarios → reescritos, y (b) atributos `data-astro-cid-*` que Astro inyecta en `<thead>`/`<tbody>`/`<tr>` → asserts ajustados a la etiqueta de apertura.
- **Verificación:** `init.ps1` verde — 252/252 tests, 26 test files (baseline previo 223/23; +3 archivos, +29 tests netos) — y `npm run build --prefix frontend-astro` verde (server + client + adapter Vercel). Aprobado por reviewer (`progress/review_18.md`, APPROVED ciclo 1, sin cambios requeridos; única observación no bloqueante: el SVG del sello lleva a la vez `role="img"` y `aria-hidden="true"`, limpieza opcional de `role="img"`).
- **Tokens consumidos:** no instrumentados en esta sesión (el arnés no registra consumo de tokens; las métricas disponibles son las de tests/build arriba).
- **Archivos tocados:** `frontend-astro/src/components/InsightCard.astro` (nuevo), `frontend-astro/src/components/MetricTable.astro` (nuevo), `frontend-astro/src/components/PartnerBiographyBlock.astro` (nuevo), `frontend-astro/tests/components/insight-card.test.ts` (nuevo), `frontend-astro/tests/components/metric-table.test.ts` (nuevo), `frontend-astro/tests/components/partner-biography.test.ts` (nuevo).
- **Notas para la próxima sesión:** Alcance acotado a los 3 componentes editoriales + sus tests; ninguno está cableado todavía en una página. Siguiente pending = feature 19 `redesign_page_home` (Phase 3: Inicio `/` con Partner Letter + About + 3 Insights + Principal Portrait; primer consumo de `EditorialHero`/`InsightCard`/`PartnerBiographyBlock` + primitivos `Container`/`Prose`/`SectionDivider`). Con la 18 cerrada, la Phase 2 (componentes editoriales) queda completa.

## 2026-06-09 — feature 19 redesign_page_home
- **Veredicto:** done
- **Resumen:** Phase 3 del rediseño editorial — primera página cableada (Inicio `/`). Se reescribió la home neón obsoleta (feature 13) al registro editorial de consultora deadpan de `docs/design/redesign_advisory_spec.md`, con EXACTAMENTE 4 bloques en orden: (1) Partner Letter = `EditorialHero` (eyebrow "Carta del Socio" + H1 serif + deck) + carta del Socio Fundador en `<Prose>` (4 párrafos serif, registro usted, voz cínica-cómplice PERSONA §3–§4), firmada "El Gurú de Bits — Socio Fundador. Operando desde los espacios muertos entre paquetes."; (2) About the Firm = `SectionDivider label="Sobre la Firma"` + párrafo institucional; (3) 3 Featured Insights = `SectionDivider label="Memos Destacados"` + EXACTAMENTE 3 `InsightCard` (títulos/extractos deadpan, fechas mono, `href="/memos"`); (4) Principal Portrait = `SectionDivider label="El Socio Principal"` + `PartnerBiographyBlock` (retrato no-facial = sello SVG a una tinta). Acento neutro Navy (sin `data-accent`, sin familia de acento). Se introdujo además `PageShell.astro` (cascarón reusable nuevo): skip-link "Saltar al contenido" + `<TopNavigation />` → un solo `<main id="main">` (landmark) con `data-accent` condicional (omitido = Navy default) → `<Footer />`. Reuso estricto de primitivos de las features 14–18 (`Layout`, `EditorialHero`, `SectionDivider`, `Prose`, `InsightCard`, `PartnerBiographyBlock`, `Container`); cero reimplementación. Solo tokens (`--accent*/--ink*/--paper*/--space-*/--step-*/--font-*/--radius/--measure`); sin gradiente/glow/dark mode/emoji/hype/exclamación/Tailwind CDN/hex de marca/prosa centrada. No se tocó `Layout.astro`, `src/lib/` ni `src/pages/api/` (diff confirmado por el reviewer).
- **Test:** dos tests espejo nuevos con el patrón doble del repo (Container API `experimental_AstroContainer` para markup + `readFileSync` del source para contrato/orden): `tests/pages/index.test.ts` (EXACTAMENTE un `<h1>`, EXACTAMENTE 3 `<article>`, los 4 bloques presentes y en orden por sus labels, `PartnerBiographyBlock` presente, firma del Socio, sin `data-accent`, sin `<img>`/`background-image`, sello SVG `aria-hidden`, 3 memos con `href="/memos"`) y `tests/components/page-shell.test.ts` (un solo `<main id="main">`, skip-link a `#main`, render del slot, chrome header/nav/footer, `data-accent` ausente sin prop y presente con burgundy/olive/forest, sin `<img>`, foco visible, sin hex/gradiente/glow en CSS). Falso positivo inicial corregido: la carta se envolvió en `<Prose>` por defecto (`<div>`, no `as="article"`) para dejar el conteo de `<article>` exactamente en 3.
- **Verificación:** `init.ps1` verde — 274/274 tests, 28 test files (baseline previo 252/26; +2 archivos, +22 tests netos) — y `npm run build --prefix frontend-astro` verde (server + client + adapter Vercel). Aprobado por reviewer (`progress/review_19.md`, APPROVED ciclo 1, sin cambios requeridos).
- **Tokens consumidos:** no instrumentados en esta sesión (el arnés no registra consumo de tokens; las métricas disponibles son las de tests/build arriba).
- **Archivos tocados:** `frontend-astro/src/components/PageShell.astro` (nuevo), `frontend-astro/src/pages/index.astro` (reescrito), `frontend-astro/tests/components/page-shell.test.ts` (nuevo), `frontend-astro/tests/pages/index.test.ts` (nuevo).
- **Notas para la próxima sesión:** Alcance acotado a la home + `PageShell` + sus tests. Siguiente pending = feature 20 `redesign_page_practica` (Phase 3: `/practica` con acento Burgundy, `ServiceCard` + `FrameworkDiagram`); primera página CON familia de acento. Observación no bloqueante del reviewer a revisar entonces: `PageShell` aplica `data-accent` en `<main>` y no en `<body>`, por lo que `TopNavigation`/`Footer` (fuera del `<main>`) conservarían el `--accent` Navy del `:root` en páginas con acento.

## 2026-06-09 — feature 20 redesign_page_practica
- **Veredicto:** done
- **Resumen:** Phase 3 del rediseño editorial — segunda página cableada y primera CON familia de acento (Burgundy). Se construyó `/practica` en registro consultora deadpan de `docs/design/redesign_advisory_spec.md`: `Layout` → `PageShell accent="burgundy"` (acento aplicado en `<main data-accent="burgundy">`, una sola familia por página; nav/footer fuera del `<main>` siguen Navy neutro) → `Container` con EditorialHero (eyebrow "Áreas de Práctica" + H1 serif + deck) → `SectionDivider label="Líneas de Servicio"` + rejilla de 4 `ServiceCard` → `SectionDivider label="El Marco"` + párrafo intro + `FrameworkDiagram`. Dos componentes nuevos: (1) `ServiceCard.astro` — `<article>` + `<h3>` serif (`--step-2`) + `<p>` descripción deadpan dentro de `--measure` + `<ul>/<li>` de entregables; borde hairline `1px var(--ink-rule)` + filete superior `2px var(--accent)`, marcador de entregable estructural (filete corto, no solo color); props `title`/`description?`/`deliverables?`; sin sombra/glow/gradiente, solo tokens, a la izquierda. (2) `FrameworkDiagram.astro` — `<figure>` + SVG inline geométrico a DOS tintas estrictas (`var(--ink)` + `var(--accent)`) que conecta las áreas de práctica (cajas hairline + conectores ortogonales hacia un nodo de convergencia rombo de acento) con el Marco de los 22 Arcanos (rejilla 11×2 = 22 dimensiones); informativo con `role="img"` + `<title>`/`<desc>` vía `aria-labelledby`; sin gradiente/sombra/glow. 4 líneas de servicio derivadas de PERSONA §5 con jerga de consultora (Diligencia Kármica, Optimización del Karma Operativo, Transformación Digital del Alma, Gestión de Deuda Técnica Espiritual); copy deadpan, trato de usted, catchphrases canónicas ("Si llegó hasta aquí, ya sabe con quién trata."; "Y si eso le tranquiliza, créalo."). Cero hex de marca; sin dark mode/emoji/hype/Tailwind CDN. Reuso estricto de primitivos (PageShell/EditorialHero/SectionDivider/Container/Prose), sin reimplementarlos. No se tocó `Layout.astro`, `src/lib/` ni `src/pages/api/` (diff sobre ambas rutas vacío, confirmado por el reviewer).
- **Test:** tres tests nuevos con el patrón doble del repo (Container API `experimental_AstroContainer` para markup + `readFileSync` del source para tokens/prohibiciones): `tests/pages/practica.test.ts` (EXACTAMENTE un `data-accent` y que es `burgundy`, un solo `<h1>`, 4 `<article>`, `<li>` de entregables, divisor "El Marco" + diagrama, orden de bloques, sin `<img>`, sin gradiente; source: PageShell burgundy, reuso de primitivos, líneas de servicio, title), `tests/components/service-card.test.ts` (`<article>`/`<h3>`, descripción y entregables condicionales, conteo de `<li>`, borde hairline + filete de acento, sin hex/gradiente/glow/`<img>`) y `tests/components/framework-diagram.test.ts` (SVG en `<figure>`, `role="img"` + title/desc, etiquetas de área, rejilla 22 dimensiones, dos tintas vía tokens, sin gradiente/sombra/`<img>`).
- **Verificación:** `init.ps1` verde — 31 test files / 304 tests, todos verdes (baseline previo 28/274; +3 archivos, +30 tests netos) — y `npm run build --prefix frontend-astro` verde (output server + adapter `@astrojs/vercel`, "Complete!"). Aprobado por reviewer (`progress/review_20.md`, APPROVED ciclo 1, sin cambios requeridos).
- **Tokens consumidos:** no instrumentados en esta sesión (el arnés no registra consumo de tokens; las métricas disponibles son las de tests/build arriba).
- **Archivos tocados:** `frontend-astro/src/components/ServiceCard.astro` (nuevo), `frontend-astro/src/components/FrameworkDiagram.astro` (nuevo), `frontend-astro/src/pages/practica.astro` (nuevo), `frontend-astro/tests/components/service-card.test.ts` (nuevo), `frontend-astro/tests/components/framework-diagram.test.ts` (nuevo), `frontend-astro/tests/pages/practica.test.ts` (nuevo).
- **Notas para la próxima sesión:** Alcance acotado a `/practica` + sus 2 componentes + sus 3 tests. Siguiente pending = feature 21 `redesign_page_metodologia` (Phase 3: `/metodologia` con acento Forest; los 22 arcanos de `lib/tarot.ts` reencuadrados como 22 dimensiones diagnósticas, sin renombrarlos, + WhitePaperLayout + TwoColorDiagram; no se modifica `lib/tarot.ts` ni endpoints). Observación no bloqueante heredada (features 19–20): `PageShell` aplica `data-accent` en `<main>` y no en `<body>`, por lo que nav/footer conservan Navy en páginas con acento — patrón canónico del repo, no bloqueante.

## 2026-06-09 — feature 21 redesign_page_metodologia
- **Veredicto:** done
- **Resumen:** Phase 3 del rediseño editorial — tercera página cableada y primera con acento Forest. Se construyó `/metodologia` como white paper institucional ("El Marco de los 22 Arcanos™", código BG/WP-001 · REV. 2): `Layout` → `PageShell accent="forest"` (acento en `<main data-accent="forest">`, patrón canónico 19–20) → `Container` → `WhitePaperLayout` con 5 secciones numeradas (Resumen ejecutivo, Fundamento metodológico, Las 22 dimensiones diagnósticas, El marco completo, Limitaciones y descargos). Dos componentes nuevos: (1) `WhitePaperLayout.astro` — portada (código de documento mono + único `<h1>` serif + deck + `<dl>` de metadatos), índice `<nav aria-label="Índice">` con anclas y números mono en `--accent`, secciones `<section id>` + `aria-labelledby` con nota al margen opcional (`<aside>`); cuerpos vía slots nombrados dinámicos resueltos con `Astro.slots.render(id)` (compatibles con la Container API; el fallback previsto en el plan no se necesitó). (2) `TwoColorDiagram.astro` — `<figure>` + SVG `role="img"` + `<title>/<desc>`, rejilla 11×2 con 22 celdas (código romano en mono fill `var(--accent)`, `<title>` por celda con el nombre canónico completo) + convergencia a rombo "DIAGNÓSTICO PRELIMINAR"; dos tintas ESTRICTAS dentro del SVG (solo `var(--ink)` y `var(--accent)`, como atributos inline para que el test lo audite). Las 22 dimensiones se derivan con `ARCANOS.map()` desde `lib/tarot.ts` sin renombrar ni reordenar (notas deadpan keyed por el string canónico literal). Copy español mexicano, registro de usted, deadpan consultora (catchphrases adaptadas: "Hoy las cartas indican que…", "…y si eso le tranquiliza, créalo."). Cero hex de marca; sin gradiente/glow/dark mode/emoji/Tailwind CDN. No se tocó `lib/tarot.ts`, `src/pages/api/*`, `Layout.astro` ni features done (perímetro confirmado por el reviewer con git status).
- **Test:** tres tests nuevos con el patrón doble del repo (Container API + readFileSync del source): `tests/components/white-paper-layout.test.ts` (11: portada/índice/secciones numeradas/slots/aside condicional/tokens), `tests/components/two-color-diagram.test.ts` (10: 22 celdas con ARCANOS real, códigos romanos, dos tintas estrictas extrayendo el bloque SVG, sin gradiente/sombra/img) y `tests/pages/metodologia.test.ts` (15: `<main data-accent="forest">` y EXACTAMENTE 1 data-accent, un solo `<h1>`, loop por las 22 entradas del ARCANOS real exigiendo nombre + "ARCANO <código>", 22 `<li class="dimension">`, índice, orden de secciones, diagrama presente, claves literales en el source). Checks de prohibiciones escopados a `<style>`/`<svg>` como en feature 20 (los comentarios en español contienen "gradiente").
- **Verificación:** `init.ps1` verde — 34 test files / 340 tests, todos verdes (baseline previo 31/304; +3 archivos, +36 tests netos) — y `npm run build --prefix frontend-astro` verde (server + adapter Vercel). Aprobado por reviewer (`progress/review_21.md`, APPROVED ciclo 1, sin cambios requeridos; observaciones no bloqueantes: border-top 3px en portada como regla editorial gruesa, border-radius inocuo en .margin-note).
- **Tokens consumidos:** no instrumentados en esta sesión (el arnés no registra consumo de tokens; las métricas disponibles son las de tests/build arriba).
- **Archivos tocados:** `frontend-astro/src/components/WhitePaperLayout.astro` (nuevo), `frontend-astro/src/components/TwoColorDiagram.astro` (nuevo), `frontend-astro/src/pages/metodologia.astro` (nuevo), `frontend-astro/tests/components/white-paper-layout.test.ts` (nuevo), `frontend-astro/tests/components/two-color-diagram.test.ts` (nuevo), `frontend-astro/tests/pages/metodologia.test.ts` (nuevo).
- **Notas para la próxima sesión:** Alcance acotado a `/metodologia` + 2 componentes + 3 tests. Siguiente pending = feature 22 `redesign_page_memos` (Phase 4: `/memos` con acento Olive; MemoCard consume GET /api/wisdom-tweet y epígrafes opcionales de /api/morpheus-quotes, sin tocar endpoints; QuarterlyArchive por trimestre; estados de carga/error en registro consultora). Con la 21 cerrada, la Phase 3 (páginas `/`, `/practica`, `/metodologia`) queda completa.

## 2026-06-09 — feature 22 redesign_page_memos
- **Veredicto:** done
- **Resumen:** Phase 4 del rediseño editorial — cuarta página cableada, acento Olive. Se construyó `/memos` (Memos Trimestrales): `Layout` → `PageShell accent="olive"` → `Container` → EditorialHero + `SectionDivider "Índice Cronológico"` + nota de registro (Prose) + `QuarterlyArchive` con 6 memorandos (BG/M-009–BG/M-014) en 3 trimestres (T2 2026 → T1 2026 → T4 2025). Arquitectura del fetch en dos capas: (a) estructura editorial server-rendered que funciona sin JS (índice completo con estados de carga visibles), (b) hidratación client-side vía `src/utils/memos-client.ts` — `hydrateMemoCards(document)` consulta GET `/api/wisdom-tweet` por tarjeta (con `?tema=` codificado) y reparte epígrafes de GET `/api/morpheus-quotes` solo en tarjetas ready; cualquier fallo de epígrafes devuelve `[]` y NO rompe los memos. El módulo cliente vive en `src/utils/` (NO `src/lib/`, que es dominio puro sin I/O según architecture.md; precedente `utils/api.js`), con fetch inyectable (`MemoFetch`) para tests sin red ni jsdom. Componentes nuevos: `MemoCard.astro` (variante de InsightCard sin borde-caja: número de memo + trimestre en mono con número en `--accent`, asunto `<h3>` serif, epígrafe opcional `<blockquote hidden>`, cuerpo `role="status"` + `aria-live="polite"` con microcopy de carga, alerta `role="alert"` oculta con microcopy de error, estado en `data-memo-state`) y `QuarterlyArchive.astro` (secciones `aria-labelledby` por trimestre, `<ol role="list">` con hairlines, agrupa preservando orden sin `.sort`). Microcopy con una sola fuente de verdad (constantes exportadas del módulo cliente, importadas por MemoCard y por los tests): carga "Memorando en deliberación…", error "El sistema oracular presenta latencia. Reintente…" — registro de usted deadpan. Endpoints intactos (solo lectura). Cero hex de marca; sin gradiente/glow/dark mode/emoji/hype/Tailwind CDN.
- **Test:** 69 tests nuevos en 4 archivos: `tests/utils/memos-client.test.ts` (24: contrato de endpoints, fetch mockeado con `vi.stubGlobal` y stubs inyectados, epígrafes tolerantes a fallo, hidratación sobre DOM falso, auditoría de registro del microcopy), `tests/components/memo-card.test.ts` (14), `tests/components/quarterly-archive.test.ts` (12: agrupación por posiciones, sin `.sort(`, mono en labels) y `tests/pages/memos.test.ts` (19: `<main data-accent="olive">` y EXACTAMENTE 1 data-accent, un `<h1>`, 3 trimestres + 6 memos, 6 status + 6 alerts, catchphrases, sin hype/exclamación). Checks de prohibiciones escopados a `<style>`/HTML renderizado.
- **Verificación:** `init.ps1` verde — 38 test files / 409 tests, todos verdes (baseline previo 34/340; +4 archivos, +69 tests netos) — y `npm run build --prefix frontend-astro` verde; confirmado en el manifest del adapter que el script de hidratación embarca inlined (clave `memos.astro?astro&type=script&index=0&lang.ts` en `inlinedScripts`). Aprobado por reviewer (`progress/review_22.md`, APPROVED ciclo 1, sin cambios requeridos; observaciones no bloqueantes: `defaultFetch` resuelto en llamada, MemoCard sin caja, `pickEpigraph` por índice global).
- **Tokens consumidos:** no instrumentados en esta sesión (el arnés no registra consumo de tokens; las métricas disponibles son las de tests/build arriba).
- **Archivos tocados:** `frontend-astro/src/utils/memos-client.ts` (nuevo), `frontend-astro/src/components/MemoCard.astro` (nuevo), `frontend-astro/src/components/QuarterlyArchive.astro` (nuevo), `frontend-astro/src/pages/memos.astro` (nuevo), `frontend-astro/tests/utils/memos-client.test.ts` (nuevo), `frontend-astro/tests/components/memo-card.test.ts` (nuevo), `frontend-astro/tests/components/quarterly-archive.test.ts` (nuevo), `frontend-astro/tests/pages/memos.test.ts` (nuevo).
- **Notas para la próxima sesión:** Alcance acotado a `/memos` + módulo cliente + 2 componentes + 4 tests. Siguiente pending = feature 23 `redesign_page_archivo` (Phase 4: `/archivo` con acento Burgundy; EngagementCard con casos ficticios + OutcomeMetricsTable con métricas deadpan; contenido editorial estático — revisar si `MetricTable` de la feature 18 sirve de base para OutcomeMetricsTable). Pendiente de dueño: smoke manual con GEMINI_API_KEY real del flujo /memos en dev/preview (docs/verification.md §3).
