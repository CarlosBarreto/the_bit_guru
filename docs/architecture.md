# docs/architecture.md — Arquitectura de bit_guru

> **Stub generado por soft-harness.** Rellena las secciones antes de empezar
> a trabajar con agentes — sin esto, el reviewer no tiene contra qué evaluar.

## Objetivo del proyecto

(1-3 líneas. ¿Qué hace y qué no hace?)

## Stack

- **Lenguaje principal:** TypeScript
- **Estructura de código:** `frontend-astro/src/`
- **Estructura de tests:** `frontend-astro/tests/`
- **Comando de tests:** `npm test --prefix frontend-astro`

## Módulos y capas

(Describe los módulos en `frontend-astro/src/` y cómo se relacionan. Si hay capas
— ej. storage → modelo → CLI — explícalo aquí.)

## Dependencias externas

(Lista las dependencias permitidas. El reviewer rechazará imports que no
estén aquí declarados.)

## Restricciones no negociables

(Cosas que el código NO debe hacer. Ej: "no llamadas HTTP en src/storage.py",
"no estado global", "no logs a stdout fuera de la CLI".)

## Decisiones abiertas

(Lo que aún no está decidido y por qué. Reduce la tentación del implementer
de decidir solo.)
